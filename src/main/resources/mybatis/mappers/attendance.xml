<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
  
<mapper namespace="attendance">

	<insert id="insertAttendance" parameterType="com.erp.vo.Attendance">
		insert into erp_attendance
		(att_no, att_emp_no)
		values
		(att_seq.nextval, #{empNo})
	</insert>

	<!--  <update id="updateAttendance" parameterType="com.erp.vo.Attendance">		
		update 
			erp_attendance
		set 
			att_work_temp_off = #{workTempOff},
			att_work_back = #{workBack},
			att_work_off = #{att_work_off},
			att_work_hours = #{}
		where att_no = #{attNo}
	</update>
	-->
	
	<!--  <select id="getAttendanceByAttNo" parameterType="map" resultType="com.erp.vo.Attendance">
		select att_no 				as attNo, 
			   att_work_day 		as attWorkDay,
			   att_work_in 			as workIn, 
			   att_work_temp_off 	as attWorkTempOff, 
		       att_work_back 		as workBack, 
		       att_work_off 		as workOff, 
		       att_work_hours 		as workHours, 
		       emp_no 				as empNo
		 from erp_attendance
		 where 	emp_no = 
		 and	work_day = 
	</select>-->

</mapper>